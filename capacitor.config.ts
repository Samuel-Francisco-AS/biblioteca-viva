import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.samuelfrancisco.bibliotecaviva",
  appName: "Biblioteca Viva",
  webDir: "dist",
};

export default config;
